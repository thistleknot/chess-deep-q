[package]
name = "rsearch"
version = "0.1.0"
edition = "2021"

[lib]
name = "rsearch"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
cozy-chess = "0.3"

[profile.release]
lto = true
codegen-units = 1
