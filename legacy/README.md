# legacy/ — retired DQN

The original Deep-Q + Russian-Doll-MCTS system (`chess_ai`, `neural_network`, `mcts`) and its
DQN-only tooling. Retired in the full realign: `main.py` now boots the spec-governed net+PUCT RL
system (see `spec/entrypoint.spec.md`). Kept for reference; not part of the live entrypoint.
